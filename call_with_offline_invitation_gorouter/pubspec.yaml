name: call_with_offline_invitation_gorouter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  zego_uikit: ^2.9.2
  zego_uikit_prebuilt_call: ^3.12.3
  zego_uikit_signaling_plugin: ^2.3.2

  cupertino_icons: ^1.0.2
  flutter_styled_toast: ^2.1.3
  device_info_plus: any
  shared_preferences: any
  faker: ^2.1.0
  cached_network_image: ^3.2.3
  go_router: ^10.1.0

dependency_overrides:
  intl: ^0.18.1
  awesome_notifications:
    git:
      url: https://github.com/Yerannnnnn/awesome_notifications.git
      ref: 0.7.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  import_sorter: ^4.6.0 # run 'flutter pub run import_sorter:main'

flutter:
  uses-material-design: true
